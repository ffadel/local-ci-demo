name: Local CI Demo

on:
  push:
    branches: [ "main", "develop" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Say Hello
        run: echo "CI running on my local ARM runner"

      - name: List files
        run: ls -la
